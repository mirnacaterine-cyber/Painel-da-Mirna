{
  "name": "painel-da-mirna",
  "version": "1.0.0",
  "private": true,
  "description": "Painel pessoal para rotina, estudos, ballet e planos.",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "check": "node --check app.js && node --check app-config.js && node --check sw.js"
  },
  "engines": {
    "node": ">=18"
  }
}
